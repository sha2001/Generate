# Use root/example as user/password credentials
version: '3.1'

services:

  mongodb:
    image: mongo:latest
    container_name: mongodb
    environment:
      - mongodb:/data/db
    volumes:
      - mongodata:/data/db
    ports:
      - 27017:27017
    command: mongod
volumes:
  mongodata: